<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %>
<body class="font-sans antialiased text-slate-800 bg-slate-50">

    <%- include('partials/navbar') %>

   <div class="bg-gradient-to-r from-emerald-500 to-teal-600 py-20 relative overflow-hidden">
        <div class="absolute inset-0 opacity-20">
            <div class="absolute top-0 right-0 w-96 h-96 bg-white rounded-full blur-3xl mix-blend-overlay"></div>
            <div class="absolute bottom-0 left-0 w-96 h-96 bg-emerald-300 rounded-full blur-3xl mix-blend-overlay"></div>
        </div>
        
        <div class="max-w-7xl mx-auto px-4 relative z-10 text-center">
            <h1 class="text-4xl md:text-5xl font-bold text-white mb-6 tracking-tight">Find Your Specialist</h1>
            <p class="text-emerald-50 text-lg mb-8 max-w-2xl mx-auto">Book appointments with top-rated doctors and specialists near you.</p>
            
            <div class="max-w-3xl mx-auto bg-white p-2 rounded-2xl shadow-2xl flex flex-col md:flex-row gap-2">
                <div class="flex-1 flex items-center px-4 border-b md:border-b-0 md:border-r border-slate-200">
                    <svg class="w-5 h-5 text-slate-400 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                    <input type="text" placeholder="Search by doctor, condition, or specialty..." class="w-full p-3 outline-none text-slate-700 placeholder-slate-400">
                </div>
                <button class="bg-emerald-600 text-white px-8 py-3 rounded-xl font-bold hover:bg-emerald-700 transition shadow-lg">Search</button>
            </div>
        </div>
    </div>

    <div class="max-w-7xl mx-auto px-4 py-12">
        <h2 class="text-2xl font-bold mb-8 text-slate-800">All Specialists</h2>
        
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% if (typeof doctors !== 'undefined' && doctors.length > 0) { %>
                <% doctors.forEach(function(doctor) { %>
                    <div class="bg-white rounded-2xl shadow-lg border border-slate-100 overflow-hidden hover:shadow-xl transition duration-300">
                        <div class="p-6">
                            <div class="flex items-start justify-between mb-4">
                                <div class="flex items-center gap-4">
                                    <div class="w-16 h-16 rounded-full <%= doctor.image %> flex items-center justify-center text-white text-xl font-bold shadow-md">
                                        DR
                                    </div>
                                    <div>
                                        <h3 class="font-bold text-lg text-slate-800"><%= doctor.name %></h3>
                                        <p class="text-emerald-600 text-sm font-medium"><%= doctor.role %></p>
                                    </div>
                                </div>
                                <div class="flex items-center bg-yellow-50 px-2 py-1 rounded-lg">
                                    <span class="text-yellow-500 text-sm mr-1">★</span>
                                    <span class="text-slate-700 text-sm font-bold"><%= doctor.rating %></span>
                                </div>
                            </div>
                            <button class="w-full bg-emerald-600 text-white py-2 rounded-lg font-medium hover:bg-emerald-700 transition shadow-md shadow-emerald-200">
                                Book Appointment
                            </button>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <p>No doctors found.</p>
            <% } %>
            </div>
    </div>

    <%- include('partials/footer') %>

</body>
</html>